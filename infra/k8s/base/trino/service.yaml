apiVersion: v1
kind: Service
metadata:
  name: trino
  namespace: trino
  labels:
    app: trino
spec:
  type: ClusterIP
  selector:
    app: trino
  ports:
    - name: http
      port: 8080
      targetPort: 8080
