apiVersion: v1
kind: Service
metadata:
  name: debezium-connect
  namespace: debezium
  labels:
    app: debezium-connect
spec:
  type: ClusterIP
  selector:
    app: debezium-connect
  ports:
    - name: rest-api
      port: 8083
      targetPort: 8083
